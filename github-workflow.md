# GitHubを建築プロジェクト記憶として使う

このメモは、Grid Plan DSL と Hermes型エージェントを、複数の建築関係者が共有する GitHub リポジトリに接続する構想である。

## 基本思想

建築プロジェクトの情報を、PDFフォルダやメール添付の集合ではなく、GitHubリポジトリとして扱う。

```text
project-house-a/
  building-card.yaml
  drawings/
    A-101.pdf
    A-201.pdf
  evidence/
    room-area.md
    openings.md
  decisions/
    design-decisions.md
  issues/
    unresolved.md
  estimates/
    quantity-takeoff.yaml
  site/
    inspection-checklist.md
```

このリポジトリが、設計者、確認申請担当、積算担当、現場監督、エージェントが共有する「プロジェクト記憶」になる。

## 役割ごとの引き継ぎ

各担当者は、同じリポジトリをクローンして、自分の職能に合ったHermesスキルで作業する。

```text
設計者
  -> building-card.yaml の初期モデルを作る
  -> 部屋、開口、用途、法規前提を整理する
  -> 確認申請に必要な不足情報を残す

確認申請担当
  -> 法規チェックに必要な項目を確認する
  -> 根拠図面と未解決事項を整理する
  -> 申請版の状態をタグやブランチで固定する

積算担当
  -> 既存の建物記憶から数量拾いを始める
  -> 仕様未確定、見積除外、単価前提を追加する
  -> 見積版の差分をPRやブランチで戻す

現場監督
  -> 設計・確認・積算で育ったデータを読む
  -> 施工上の干渉、発注漏れ、検査項目を確認する
  -> 現場で発生した是正、質疑、変更を追記する
```

後工程ほど、ゼロから細かく入力する必要はない。設計段階で作られた基本データを受け取り、各担当者は自分の観点で確認・追記・修正する。

## GitHubが母艦になる理由

GitHubを使うと、建築情報の引き継ぎに必要な性質が自然に得られる。

- 差分が残る
- 誰が何を変えたか残る
- ブランチで役割ごとの作業を分けられる
- Pull Requestで変更をレビューできる
- Issueで未解決事項を管理できる
- Actionsで自動チェックを走らせられる
- タグやReleaseで申請版、見積版、着工版を固定できる

## ブランチ運用例

```text
main
  合意済みの建物記憶

design/intake
  設計者による初期入力

permit/check
  確認申請用の法規・根拠整理

estimate/takeoff
  積算担当による数量・仕様整理

site/construction
  現場監督による施工・検査メモ
```

小規模案件では、ブランチを増やしすぎず、`main` と `issues/` だけでもよい。重要なのは、建物記憶と判断履歴が同じ場所に残ることである。

## エージェントの役割

エージェントは、各担当者のローカル環境やクラウド環境で、同じリポジトリを読み書きする。

```text
同じGitHubリポジトリ
  building-card.yaml
  evidence/
  decisions/
  issues/

設計者のHermes
  designer skill

積算担当のHermes
  estimator skill

現場監督のHermes
  supervisor skill
```

同じ建物記憶を使うが、エージェントの振る舞いは担当者の役割によって変える。

## 入力負荷の考え方

設計初期は情報が少ないため、設計者とエージェントが対話しながら基本モデルを育てる。

工程が進むほど、建物記憶は充実している。積算担当や現場監督は、ゼロから入力するのではなく、既存データを確認し、足りない情報だけを補う。

```text
設計初期: 入力が多い、仮説も多い
確認申請: 根拠確認と不足整理が中心
積算: 数量・仕様・除外条件の追加が中心
施工: チェックリスト、干渉、変更履歴の追加が中心
```

現場監督の段階では、細かな手入力を要求しすぎない。エージェントは既存の建物記憶からチェックリストや確認項目を生成し、監督は現場で発生した差分・是正・質疑を短く追記できるようにする。

## 長期的な姿

建築プロジェクトは、図面PDFの置き場ではなく、構造化されたプロジェクト記憶を持つリポジトリになる。

Agent AIはそのリポジトリを読み書きし、設計、確認申請、積算、施工の各担当者が同じ建物記憶を引き継ぐ。
