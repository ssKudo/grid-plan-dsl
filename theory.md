# Conversational Grid Plan DSL: 研究ノート

本ドキュメントは、Agent AIとの対話を通じて住宅図面をテキストで記述し、住宅法規チェック用の建物情報へ構造化する軽量表現手法「Conversational Grid Plan DSL」に関する研究メモである。

## 1. コンセプトと目的
- **目的**: BIM/IFCや高精度なPDF OCRに依存せず、汎用Agent AIとCLI環境のみで、図面読解結果を整理・構造化する。
- **アプローチ**: 人間が図面を見ながらAgentに説明し、Agentがそれを `node / edge / cell` のグリッドグラフとして構造化する「対話型建物モデル構築」。
- **核心的アイデア**: PDF OCRを「抽出」ではなく「根拠照合・矛盾検出」に利用することで、認識精度の問題を検証問題に変換する。
- **位置づけ**: CADやBIMを置き換えるものではなく、法規チェック前処理に必要な情報を軽量に記述するテキスト作図レイヤーである。

## 1.1 仮説
建築図面の完全自動認識を前提にするよりも、実務者が読み取った内容をAgent AIが逐次構造化し、ASCII図で確認しながら修正する方が、小規模な住宅案件では低コストかつ説明可能な入力データを得やすい。

## 2. 提案手法 (Grid Plan DSL)

### 2.1 建物カルテ (Building Card)
建物情報をMarkdown（人間用）とYAML（AI/判定器用）で管理する。
- **プロジェクト属性**: 用途、構造、モジュール（例: 910mm）
- **敷地属性**: 面積、接道幅員、用途地域

### 2.2 通り芯グリッドモデル
日本の木造住宅に一般的な通り芯（X:数字, Y:かな）を共通言語とする。
- **Node (点)**: 通り芯の交点。
- **Edge (線分)**: 交点同士を結ぶ線。壁、開口、建具、境界を定義。
- **Cell (区画)**: 4つのエッジに囲まれた最小単位。部屋、設備、用途を定義。

Cell IDは左下側のY/Xラベルで代表する。たとえば `い-1` は、`い-ろ` 間かつ `1-2` 間に囲まれる区画を表す。Edgeは `い通り 1-2間` のように、通り名と隣接スパンで指定する。

### 2.3 表現例
- **外形**: 原点からの方向と長さの列（例: 右へ11890, 上へ3000...）で表現。
- **部屋**: セルの集合として定義。
- **開口**: エッジに紐づくテンプレート（例: 16520）として定義。

### 2.4 ASCIIアートによる認識確認
Agentの内部認識をASCIIアートで出力し、ユーザーが即座に修正できるフィードバックループを構築する。

### 2.5 断面グリッドへの拡張
平面グリッドを高さ方向（Z軸）へ拡張し、断面プロファイル（屋根形状など）を定義することで、日影や斜線制限の判定へ繋げる。

## 3. 法規チェックへの接続フロー
`User + Agent対話` $\rightarrow$ `Grid Plan DSL` $\rightarrow$ `building-card.yaml` $\rightarrow$ `ASCII確認` $\rightarrow$ `PDF根拠照合` $\rightarrow$ `決定的なルールエンジンによる判定` $\rightarrow$ `根拠付きレポート`

## 4. 期待される貢献と限界
- **貢献**: 小規模実務者が導入コスト低く法規チェック準備を行える。
- **新規性**: OCRを主要な入力源ではなく、構造化済みモデルの検証器として使う点。
- **限界**: 直交グリッド前提のため、斜め壁や複雑な不整形建築には追加表現が必要。

## 5. 参照元資料
- `sandbox/docs/position-paper.md`
- `sandbox/docs/conversational-grid-plan-dsl.md`
