# 建物情報構造化ワークフロー: Conversational Grid Plan DSL

Agent AIを用いて、実務者が読み取った建築図面情報から法規チェック用の構造化データ（建物カルテ）を作成するための実務的な運用フロー。

## 1. 構築プロセス
1. **外形定義**: 左下等を原点とし、時計回りに辺の方向と長さ（mm）を指示する。
2. **グリッド定義**: 通り芯（X:数字, Y:かな）とモジュール（通常910mm）を定義する。
3. **部屋・設備配置**: 
   - 「い-1からに-4まで和室」のように、通り芯の範囲で部屋を指定。
   - 設備は特定のセル（例: ろ-4）に配置。
4. **開口・建具配置**: 
   - 「い通り2-3間に掃出し16520」のように、エッジ（通り芯の間）に建具を指定。
5. **ASCII確認と修正**: Agentが出力したASCII平面図を見て、誤認があれば口頭で修正させる。
6. **PDF根拠照合**: 作成したカルテの内容をPDF図面（平面図、建具表等）と照合し、根拠を付与または矛盾を修正する。

## 2. データ構造 (YAML)
- **Outline**: `path: [ {dir: E, len: 11890}, ... ]`
- **Grid**: `x: [1, 2, ...], y: [い, ろ, ...]`
- **Rooms**: `id, name, type, cells: [い-1, い-2, ...]`
- **Openings**: `id, type, template, edge: "い通り 1-2間", room: "washitsu"`

## 3. セルとエッジの指定ルール
- **Cell ID**: `い-1` は `い-ろ` 間、`1-2` 間に囲まれる区画を表す。
- **Cell Range**: `い-1からは-3まで` は、Y方向 `い, ろ, は` とX方向 `1, 2, 3` の直積で得られるセル集合を表す。
- **Horizontal Edge**: `い通り 1-2間` は、Y軸の `い` 通り上で、X軸の `1` から `2` の間にあるエッジを表す。
- **Vertical Edge**: `2通り い-ろ間` は、X軸の `2` 通り上で、Y軸の `い` から `ろ` の間にあるエッジを表す。

## 4. PDF OCRの活用ルール
- **NG**: PDFから正解をすべて自動抽出させようとする。
- **OK**: 「カルテのLDK面積 18.2m2が、PDFのA-201図面にあるか確認して」という検証として使う。

## 5. ゴール
法規判定エンジンが処理可能な、決定的な入力データ（building-card.yaml）を完成させること。
